{
  "name": "schema_types_generator",
  "version": "1.0.0",
  "description": "A lib for generating DB schema types for typescript",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Valentine Oragbakosi",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.6.5",
    "mongodb-uri": "^0.9.7"
  },
  "devDependencies": {
    "@types/node": "^14.14.37"
  }
}
